#IDEs
.vscode
.idea

#Linters and coverages
.nyc_output/
reports
coverage

#Nodejs
node_modules
out

#Local data
*.sqlite

# Exclude tests — but re-include the two paths that src/database/seed.ts imports
# from directly (anti-pattern; these should be moved to src/ in a future refactor).
test/
!test/seed
!test/seed/**
!test/setup.ts

#Secrets and local config
.env
.env.*
*.key
*.pem

#Git
.git
.gitignore
.gitattributes
.github

#Docs
docs/

### Docker files
Dockerfile
.dockerignore
docker-compose.yml