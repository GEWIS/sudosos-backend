apiVersion: v1
kind: Secret
metadata:
  name: backend-jwt
  namespace: sudosos-test
type: Opaque
stringData:
  # RSA private key PEM — generate with: openssl genrsa 2048
  JWT_PRIVATE_KEY: |
    REPLACE_ME
